# EchoFarm 项目面经

> 使用边界：以下表述基于当前仓库可核验实现。只有在你能够独立运行 Demo、解释源码并承担实现细节后，才建议写入个人简历。项目尚未在本机完成真实 Stardew Valley + SMAPI 编译、玩家实测或 Nexus 发布，因此不能声称“已上线”“已有用户”或“线上指标提升”。

## 1. 项目简介（简历可用）

EchoFarm 是一个 Go + CloudWeGo Eino 与 C#/SMAPI 组合的 AI 原生游戏 Mod：它从玩家多日真实操作中提取有证据的行为习惯，形成版本化玩家画像，并驱动半透明 AI 分身在动态农场中与玩家并行行动。系统通过结构化模型输出、确定性证据合并、实时目标避让、跨进程幂等账本和双重安全校验，让 AI 决策既具有泛化能力，又不会直接越权修改游戏状态。

## 2. 简历 bullet

- **持续学习架构：** 针对单次示范容易把偶然操作固化为长期偏好的问题，将模型职责收敛为提取带情境与事件引用的行为观察，并由确定性合并器完成置信度增长、同情境冲突衰减和跨天气隔离，形成按存档版本化、可追溯且可重复学习的玩家画像；通过三日教学脚本验证画像修订递增和稳定习惯不被雨天样本误覆盖。
- **人机协同决策：** 针对玩家与 AI 化身同时行动时可能争抢同一作物的问题，引入二十秒语义活动窗口、玩家意图推断和目标声明机制，将近期浇水、收获、存箱行为汇入决策上下文，并在模型输出后执行确定性冲突校验；四天跨进程 Demo 可验证 Echo 识别玩家正在浇水并主动选择未占用的收获目标。
- **跨进程一致性：** 针对 C# 游戏线程与 Go 推理服务之间的超时、重放和状态漂移风险，以存档、会话、快照版本组成关联键，建立学习结果和动作决策幂等语义，并要求动作结果与账本中的最终动作完整匹配；重复请求不会再次调用模型或重复提高画像置信度，过期与伪造结果会在进入游戏执行前被拒绝。
- **分层安全执行：** 针对大模型输出不可直接信任的问题，将 LLM 限制在 Trait、Intent 和白名单高层动作三类结构化输出，Go 层校验目标存在性、天气、体力和资源条件，C# 层在最新快照上二次校验并只在游戏主线程执行；模型或本地服务故障时仅停止 Echo，不阻塞游戏保存与退出。
- **可观测与交付工程：** 针对 AI 决策难解释、Mod 安装链路复杂的问题，使用 SQLite 保存学习修订、候选动作、最终动作和执行结果，并提供 F9 游戏内记忆面板；同时实现 sidecar 自动拉起、健康检查、进程归属清理和 Windows/Linux/macOS 四平台打包验证，当前 Go race/vet、77 个 .NET 测试及双 Demo 均可本地复现。

## 3. 面试问题

### 主题一：产品定义与总体架构

#### 1. 为什么这个项目不是普通的游戏 Agent 助手？

口播：我一开始就没有把它设计成聊天框或攻略问答，因为那类功能和游戏世界的关系很弱。EchoFarm 的输入是玩家真实操作形成的事件流，输出也不是文本建议，而是一个能进入游戏、独立移动和操作作物的 AI 化身。它学习目标、顺序、箱子偏好和体力边界，再根据当天天气、作物状态和玩家实时动作重新决策。最关键的区别是它不替玩家按固定宏，也不接管角色，而是作为第二个行动主体与玩家并行工作。当前仓库可以用四天脚本证明跨天画像演化和协作避让，但真实游戏内体验仍需要合法游戏环境做最终验证。

#### 2. 为什么采用 Go sidecar 加 C#/SMAPI，而不是全部写在 C# Mod 里？

口播：这个选择来自运行边界。SMAPI 的官方集成面在 .NET，因此监听输入、读取世界状态、绘制角色和修改作物必须留在 C# 游戏进程内；而持续学习、Eino 编排、SQLite 记忆和 HTTP 可观测接口更适合放在独立 Go 服务。这样 AI 依赖和游戏程序集解耦，Go 侧可以脱离游戏做 race 测试和跨进程 Demo，C# 侧保持薄适配器。代价是需要处理进程生命周期和协议一致性，所以我又实现了 sidecar 自动启动、健康检查、只终止自有进程以及三元关联键。这个方案不是为了堆两种语言，而是让每种语言承担最合适且可隔离测试的职责。

#### 3. 整条请求链路是什么？

口播：教学时，玩家按 F7 后，SMAPI 侧把移动和浇水、收获、存箱等语义动作记录为带前后状态差异的示范；Go 收到后先分段，再由 Eino 提取本次 Trait 观察和技能，确定性合并器更新长期画像，最后在一个 SQLite 事务中保存示范、画像、技能和修订。执行时，C# 生成当前世界快照并附带最近玩家活动，Go 推断玩家意图、生成目标占用集合，再让动作图选择互补任务。动作通过 Go 校验后返回 C#，C# 再基于最新状态校验、寻路和执行，结果回到 Go 写入账本并决定下一步或重规划。

追问 1：为什么不让大模型一次生成完整计划？

口播：完整计划在一个静态世界里很直观，但游戏状态会持续变化：玩家可能先处理某株作物，天气和背包状态也可能在执行期间改变。如果一次生成十步，后续步骤很快就会过期，还会放大模型幻觉的影响。我选择一次只生成一个高层动作，逐格移动和动画由确定性逻辑完成，动作结束后用新快照继续决策。这样模型调用次数会增加，延迟和成本需要后续实测，但每个决策都能绑定准确的世界版本，也能在失败后只重规划剩余工作，而不是重放整段过时计划。

追问 2：为什么没有使用 RAG？

口播：这个问题的知识源不是外部文档，而是当前存档的结构化世界状态、玩家的行为轨迹和已经形成的画像。把这些内容放进向量库再检索，不会自然解决状态一致性、目标冲突或动作安全，反而增加召回不确定性和中间件复杂度。因此我直接用 SQLite 保存强结构化证据，用 Eino 处理语义推断，并通过明确的数据契约把上下文送给模型。如果后续要支持百科问答或大型 Mod 规则库，RAG 才可能成为独立能力；当前主链路刻意不引入它。

### 主题二：持续学习与模型边界

#### 4. 为什么从“生成完整画像”改成“生成 Trait Patch”？

口播：完整画像由模型重写时，模型既负责理解当前示范，又负责长期状态合并，两个职责混在一起会导致一次异常操作覆盖已有习惯，也很难做幂等和审计。我把模型输出缩小为本次观察：习惯键、候选值、天气情境、证据事件和本次强度。之后由 Go 的合并器按照固定公式更新观测次数、冲突次数和置信度，并从最强 Trait 投影出运行期快捷字段。这样 AI 仍承担不可替代的语义理解，但状态演化规则可测试、可回放、可解释，模型更换也不会改变数据库的一致性语义。

#### 5. 置信度如何更新，为什么这样设计？

口播：首次观察的置信度最多为 0.75，避免单个样本直接成为绝对事实；同值同情境再次出现时使用 `1-(1-old)*(1-strength*0.5)` 递增，让早期证据提升明显、后期逐渐收敛但不会超过一。相同键和情境出现不同值时，旧候选置信度乘 0.8 并增加冲突次数。这个公式不是统计学真值，而是一个透明、单调、易测试的工程策略，目标是展示稳定性和可解释性。真实玩家数据到位后，还需要依据收敛速度、误判率和行为漂移情况重新标定参数。

#### 6. 如何保证模型不能伪造学习证据？

口播：每次教学请求先建立当前示范事件 ID 的集合，Eino 输出的每个 Trait 和技能都必须引用这个集合里的真实 ID。任何未知 key、非法情境、越界强度或不存在的事件引用都会被识别为无效模型输出，整个学习事务不会写入 SQLite。持久化时事件 ID 会加上 demonstration ID 形成全局可追溯引用，解决不同日期可能复用局部事件 ID 的问题。最终 HUD 展示的是这些结构化证据的投影，不允许模型临时编写自由文本人物小传，因此面试时可以从一个画像结论一直追到原始游戏动作。

追问 1：为什么天气要进入 Trait 的上下文？

口播：如果没有上下文，晴天“先浇水后收获”和雨天“直接收获”会被视为同一个任务顺序的矛盾，系统可能错误地降低玩家稳定习惯的置信度。实际上雨天跳过浇水是环境约束，不是偏好漂移。因此我把天气作为最小情境维度，只有键和情境都相同但值不同时才计冲突。当前四天 Demo 会检查雨天教学后晴天任务顺序的冲突数仍为零。这个设计也保留扩展空间，但没有提前引入季节、地点、节日等尚未被当前场景证明需要的维度。

追问 2：怎样处理重复请求和模型重试？

口播：教学请求使用存档 ID 与 demonstration ID 作为幂等键。服务在调用 Eino 前先查询学习修订，如果该示范已经成功提交，就直接返回当时保存的完整结果，不再次推理，也不再次增加观测次数。首次处理时，示范、合并后的画像、技能和学习变化在同一个事务中提交，避免只写入一半。这样网络超时后的客户端重试是安全的。模型调用本身失败时不会落任何长期状态，也不会静默切换到 fixture 假装完成真实学习。

### 主题三：实时协作与规划

#### 7. Echo 如何知道玩家当前在做什么？

口播：我没有上传逐帧坐标或整段录像，而是在 C# 侧维护最近二十秒的语义活动窗口，只记录成功的浇水、补水、收获和存箱事件及目标 ID。窗口会淘汰过期事件，并把同类同目标的重复动作压缩为最新一条。Go 收到快照后，将活动和长期画像交给独立的 Eino 意图图，输出 watering、harvesting、depositing 或 unknown 之一，同时引用真实目标作为证据。这种做法比仅看当前位置更可靠，也比传全量操作流更节省上下文，并且保持了明确的数据最小化边界。

#### 8. 如何保证 Echo 不与玩家抢同一个目标？

口播：这个约束分两层实现。第一层把近期成功操作的目标整理成 player-claimed targets，并把过滤后的可用目标与推断意图一起放进动作模型上下文，让模型主动选择互补任务。第二层不信任模型是否遵守提示词，动作返回后由确定性协调器再次检查目标 ID；如果模型仍选择玩家占用目标，就把最终动作改成安全停止，并把原候选与最终结果都记录到账本。双层设计既保留模型的情境推理能力，又保证“不要抢活”不是一句无法验证的 Prompt 约定。

#### 9. 为什么使用短时窗口，而不是长期记录玩家每一步？

口播：协作意图是强时效状态，玩家二十秒前浇过一株作物，并不意味着他整个上午都只想浇水。把所有历史动作送给模型会让旧行为淹没当前意图，也会增加上下文和隐私负担。因此 Bridge 只保留短时语义事件，移动帧不进入窗口，Go 还会依据当前 tick 再过滤一次。长期习惯则通过教学和 Trait 画像保存，两类记忆分别解决“这个玩家通常怎么做”和“他现在正在做什么”，避免把长期人格与瞬时协作状态混成同一个变量。

追问 1：玩家行为变化得很快，如何避免旧决策落地？

口播：每个快照都有单调版本，并且近期活动也参与快照指纹；玩家的新操作会使后续快照版本变化。Go 返回的动作必须回显存档、会话和快照版本，C# 收到后用最新状态再次检查。如果网络响应已经过期、目标消失或条件变化，动作不会执行。服务端同样以三元组记录决定，相同快照的重试返回同一个最终动作。这个机制不能消除模型延迟，所以后续仍需测量真实 P95，但它能把延迟造成的错误限制为“本轮停止或重试”，而不是污染游戏状态。

追问 2：如果所有目标都被玩家占用，为什么选择停止而不是等待？

口播：当前执行协议是一问一答的高层动作，没有独立的长时间等待动作。如果所有合法目标都处于玩家声明窗口内，继续选择只会产生争抢，伪造一个移动目标又会破坏白名单边界，因此安全行为是返回 stop_session，并明确记录“玩家正在处理目标”。这是有意的保守策略。后续若实测发现停止过于频繁，可以增加带截止时间的 wait 动作或短退避，但必须把会话取消、保存游戏和超时语义一起设计，不能用无限循环轮询代替。

### 主题四：一致性、安全与故障处理

#### 10. 为什么要同时在 Go 和 C# 做安全校验？

口播：Go 侧掌握模型输入和完整业务语义，适合拒绝不存在的目标、雨天浇水、空水壶继续浇水或空背包存箱等逻辑错误；但网络传输后到真正执行前，游戏世界仍可能变化，所以 C# 侧必须基于最新快照再次校验关联字段和动作前置条件。两层不是重复浪费，而是不同时间点的信任边界：服务端约束 AI，客户端保护游戏。如果 Go 进程、网络或模型异常，C# 状态机只隐藏 Echo 并回到空闲状态，不让异常冒泡到游戏保存和退出流程。

#### 11. 动作账本解决了什么问题？

口播：没有账本时，同一个快照因网络重试可能再次调用模型并得到不同动作，动作结果也可能关联到不存在或已变化的决定。现在系统用存档、会话和快照版本作为唯一键，先持久化 AI 候选、玩家意图、占用目标和最终安全动作，再允许结果回填。重复的 next-action 请求直接读取旧决定；结果必须与账本中的最终动作完整匹配，重复相同结果是幂等的，不同结果会被拒绝。这个账本同时支撑 F9 解释界面和故障排查，而不是单纯为了记录日志。

#### 12. SQLite 事务边界如何划分？

口播：学习路径中，原始示范、画像新版本、技能新版本和 learning revision 必须同成同败，所以它们放在一个事务中；如果模型输出验证失败或任何写入失败，事务回滚，不会出现画像已经更新但技能仍是旧版本的状态。运行路径中，每个决策记录独立幂等写入，动作执行完成后再附加结果，因为执行发生在游戏进程，无法与 SQLite 做分布式事务。两阶段之间依靠关联键和完整动作比对保证一致性，这是一种面向本地单机场景的轻量事件账本设计。

追问 1：服务崩溃在动作执行前后分别会怎样？

口播：如果服务在返回动作前崩溃，C# 收不到结果，会超时并停止 Echo，游戏本体继续；如果决定已落账但响应丢失，相同快照重试会读取同一个动作，不会再次调用模型。如果 C# 已执行动作但结果尚未回传时服务崩溃，账本保留未完成决定，当前实现不会猜测执行结果，Echo 会在连接失败后停止。重启后的自动恢复只恢复玩家长期记忆，不自动重放未确认动作，避免重复收获或重复存箱。更复杂的补偿机制需要真实游戏幂等 API 支持，目前选择安全停机。

追问 2：为什么模型不可用时不自动降级到 fixture？

口播：fixture 的目的只是让 CI 和演示可复现，它根据固定规则生成结果，并不代表真实模型已经理解了玩家。如果生产模式连接失败却自动切到 fixture，用户会误以为 Echo 仍在学习，实际上画像质量和决策来源已经改变，属于静默语义降级。项目因此把模型不可用映射为明确的 503，Bridge 捕获后停止并隐藏 Echo，原存档和已有记忆不受影响。离线 fixture 只能由配置显式选择，这保证演示能力和真实 AI 能力不会被混淆。

### 主题五：工程质量与发布

#### 13. Go sidecar 如何做到对普通 Mod 玩家透明？

口播：发布包会按 Windows x64、Linux x64、macOS Intel 和 Apple Silicon 分别内置一个裁剪后的 Go 可执行文件。Mod 启动时先探测配置的 localhost 服务；如果已有健康服务就复用，否则只启动当前 Mod 包内对应平台的 Core。Supervisor 记录进程归属，退出时只终止自己创建的进程，避免误杀用户手动运行的服务；Unix 平台还会恢复可能被解压工具移除的执行位。模型密钥只从启动 SMAPI 的环境继承，不进入 config、日志、数据库或压缩包。

#### 14. 你如何证明这不是只写了架构文档？

口播：仓库有三层可重复证据。第一层是 Go 单元和 race 测试，覆盖画像合并、Eino 输出约束、协调器、SQLite 事务、HTTP 与策略；第二层是 77 个 .NET Bridge 测试，覆盖 JSON 契约、录制状态机、网络关联、安全门、进程托管、背包和箱子转移；第三层是两个真实跨进程脚本，其中 Continuum 脚本启动 Go 服务、连续提交三天教学、发起第四天协作决策并从 memory API 验证账本。四平台打包烟测也会逐个检查 ZIP 根目录、DLL、平台二进制和敏感文件排除。真实游戏内编译与试玩尚未完成，我会明确区分。

#### 15. 这个项目当前最大的风险和下一步是什么？

口播：最大风险不是 Go 逻辑，而是缺少真实 Stardew Valley 1.6 与 SMAPI 环境后的最后一公里验证。SMAPI 类型和游戏行为只能做源码级审计，无法证明每个绘制、作物收获和箱子交互在目标版本都完全兼容；同时真实模型的延迟、成本和输出稳定性还没有量化。下一步应在合法游戏目录完成 Release 编译，用一次性存档跑教学、共同劳动、存箱、满箱和保存退出场景，再记录模型 P50/P95、每次决策 token 与动作成功率。通过后才能生成真实可玩 ZIP 和 Nexus 页面，现阶段不能把 fixture 包说成上线版本。

追问 1：如果目标数量很多，当前方案会遇到什么性能问题？

口播：目前世界快照会包含可见作物、水源、箱子和最近玩家活动，目标规模增大时 JSON 体积、模型上下文和决策延迟都会线性上升，逐动作调用模型也会放大成本。当前实现优先保证语义正确和可解释，还没有声称大农场下的性能收益。可验证的优化方向是先在 Go 侧做确定性候选压缩，例如按动作可行性、距离和目标状态筛出有限候选，再让模型在候选间做偏好权衡；同时记录目标数量、请求字节、推理耗时和 token，建立 50、200、1000 目标三档基线后再决定是否缓存。

追问 2：发布时如何避免把不该发布的东西打进 Mod？

口播：打包脚本从已构建的两个 Mod DLL 和指定平台重新编译的 Go Core 开始，在临时目录构造唯一的 `EchoFarm/` 根目录，并只复制许可、玩家说明、manifest 和运行文件。验证脚本会检查平台可执行文件名、根目录结构、版本与 Nexus update key，同时拒绝数据库、日志、PDB、游戏程序集和环境文件。CI 使用 fixture DLL 只验证包结构，输出随后删除，文档明确禁止把它当成可玩文件。真实包必须在合法游戏环境构建后再过相同验证，并由人工核对截图、权限和 AI 披露。

## 4. 源码证据索引

| 主题 | 关键路径与内部符号 | 对应正文位置 |
| --- | --- | --- |
| 多日 Trait 提取 | `echofarm-core/internal/intelligence/learning_graph.go`、`LearningInference` | 问题 4、6 |
| 确定性画像合并 | `echofarm-core/internal/modeling/merger.go`、`Merge` | 问题 4、5 |
| 学习幂等与事务 | `echofarm-core/internal/learning/service.go`、`TeachOutcome`；`internal/memory/sqlite.go` | 问题 6、12 |
| 玩家意图 | `echofarm-core/internal/intelligence/intent_graph.go`；`PlayerActivityWindow.cs` | 问题 7、9 |
| 目标避让 | `echofarm-core/internal/coordination/service.go`、`ValidateChoice` | 问题 8 |
| 动作账本 | `echofarm-core/internal/policy/service.go`、`DecisionRecord`、`decision_records` | 问题 10、11 |
| C#/Go 关联 | `EchoFarmClient.cs`、`EchoSession.cs`、`ActionSafetyGate.cs` | 问题 3、10、11 |
| 游戏主线程执行 | `StardewGamePort.cs`、`GridPathfinder.cs`、`HarvestTransfer.cs`、`DepositTransfer.cs` | 问题 3、10 |
| 记忆面板 | `internal/memoryview/service.go`、`EchoMemoryPresenter.cs`、`EchoMemoryOverlay.cs` | 问题 1、11 |
| Sidecar 生命周期 | `CoreProcessSupervisor.cs`、`SystemCoreProcess.cs` | 问题 2、13 |
| 四天验收 | `demo/run-continuum-demo.sh`、`demo/fixtures/day-*-*.json` | 问题 1、5、7、14 |
| 跨平台打包 | `scripts/package-nexus.sh`、`scripts/verify-nexus-package.sh` | 问题 13、14 |
